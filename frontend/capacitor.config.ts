import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.receiptocr',
  appName: 'ReceiptOCR',
  webDir: 'dist/frontend/browser'
};

export default config;
